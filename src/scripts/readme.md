# Scripts in src

Each script is responsible for one method, and generates callable executables that require, or not, parameters to be executed. They are called by the main script which has its own GUI.